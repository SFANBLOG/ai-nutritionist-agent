# ============================================
# AI 营养师 Agent - 后端依赖(含 BGE / Milvus / MinIO)
# 用于 Docker 镜像与完整功能部署
# ============================================
# Web 框架
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9

# ORM / 数据校验
sqlalchemy>=2.0.30
pydantic>=2.7.0
pydantic-settings>=2.2.0
email-validator>=2.1.0

# 认证
python-jose[cryptography]>=3.3.0
bcrypt>=4.0.1

# LLM / Agent
langchain>=0.2.0
langchain-core>=0.2.0
langchain-openai>=0.1.0
langgraph>=0.1.0
openai>=1.30.0

# 向量化(BGE 本地模型)
sentence-transformers>=2.7.0

# 向量库(Milvus)
pymilvus>=2.4.0

# 对象存储(MinIO)
minio>=7.2.0

# PDF 文本抽取(可选,用于上传 PDF 体检报告)
pypdf>=4.0.0

# 工具
python-dotenv>=1.0.0
httpx>=0.27.0

# MySQL 支持(使用 MySQL 时必需;仅用 SQLite 可忽略)
pymysql>=1.1.0
cryptography>=42.0.0
