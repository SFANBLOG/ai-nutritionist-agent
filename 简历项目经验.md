# 简历项目经验 —— 智膳参谋（AI 营养师 Agent）

> 以下内容全部基于项目真实代码与实测记录撰写，未虚构任何数据；可直接粘贴进简历后按版式微调。

---

## 智膳参谋 · 基于 LangGraph 多 Agent 的个性化饮食管理平台

**算法工程师（全栈开发）**

**项目描述**：针对体检指标异常人群普遍面临的指标无解读、饮食建议空泛难执行、AI 自动生成内容不可控问题，本项目致力于打造一款"上传体检报告 → 多 Agent 协作分析 → 输出可执行个性化食谱"的一体化平台，通过 RAG 检索增强（每条建议标注证据来源与等级）与人工确认闸门（HITL），提供有据可查、可控可信、可直接落地的个性化饮食管理服务。

**技术栈**：LangGraph、LangChain、FastAPI、Milvus、BGE（bge-small-zh-v1.5）、MinIO、MySQL 8.0、SQLAlchemy 2.0、Pydantic v2、Vue 3、Element Plus、Pinia、TailwindCSS、Docker Compose

**工作内容**：

- **多 Agent 工作流编排**：基于 LangGraph 状态图编排「健康分析 → 营养规划 → 食谱生成 → 质量审核」四个 Agent，实现条件回边——审核不通过自动返工（上限 3 轮），并完整记录每个 Agent 的执行日志供前端追溯。
- **RAG 知识库建设**：构建覆盖血糖/血压/尿酸/血脂/膳食指南/慢性肾病等 20+ 主题的 28 条营养学知识库，采用 BGE 中文向量模型（512 维）本地向量化 + Milvus 向量库语义检索，每条知识标注**证据等级（A/B/C）**，生成方案与食谱时逐条标注来源文献，实现结论可溯源。
- **体检报告结构化解析**：开发规则解析引擎，自动抽取血糖/血压/尿酸/胆固醇/甘油三酯等核心指标并分级评估、生成风险标签；配套编写 60 份覆盖代谢综合征、高尿酸、糖尿病高血压等 16 种健康状况的模拟报告及自动化校验脚本（verify_reports.py），回归验证解析正确性。
- **HITL 人工确认闸门**：设计食谱审核双通道——开启闸门时食谱进入 pending_review 状态，用户可确认采用或提交修订意见触发重做（记录 revision_count）；关闭时回退为自动重做模式，杜绝无监督自动放行。
- **工程化与降级设计**：体检报告文件统一经 MinIO 对象存储（支持 txt/md/pdf 文本抽取）；数据库/向量库/对象存储/embedding 四层均实现自动降级（MySQL→SQLite、Milvus→进程内检索、MinIO→本地磁盘、BGE→兜底向量化），保证零外部依赖也能跑通全链路；未配置 LLM Key 时自动切换规则引擎模式。
- **全栈交付**：FastAPI 实现 JWT 认证、报告/食谱/偏好等 REST 接口，多日食谱按 recipes → daily_menus → meals → dishes 四级结构化落库；Vue 3 + Element Plus 实现 11 个页面；Docker Compose 一键编排 etcd/MinIO/Milvus/MySQL/后端/前端 6 个服务。
- **多周期食谱与菜品轮换**：实现一周（7 天）/ 一月（30 天）/ 自定义（最长 90 天）三种周期生成（`MENU_MAX_DAYS=90` 校验上限，`cycle_type` 落库）；内置 7 天主菜轮换菜品池（`day_index % 7`），保证一周内每日主菜不重复，并自动净化健康状况禁忌食材，提升方案实用性与可坚持性。

**项目成果**：

- **技术指标**：规则引擎模式实测全链路跑通——5 项指标 100% 解析并分级；RAG 召回 4 条带证据等级知识；生成 2018 kcal（目标 2300）食谱，**审核首轮即通过（返工 0 轮）**；60 份 / 16 种模拟报告回归全通过；Docker Compose 一键编排 6 服务拉起完整链路并公网可访问。
- **业务指标**：知识库覆盖血糖/血压/尿酸/血脂/慢性肾病等 20+ 主题、28 条营养学知识（每条标注证据等级），可服务代谢综合征、糖尿病、高尿酸等 16 类慢病人群；通过 HITL 人工确认闸门杜绝无监督自动放行、RAG 证据标注实现建议可溯源，从机制上保障生成内容安全可信；修复规则审核假阳性（建议文本被误判为违规食材导致全部退回），使审核结果与实际菜品一致，实测审核由"全部退回"修复为首轮通过。
